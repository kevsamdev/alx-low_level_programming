#include "main.h"
/**
* _isupper -program checks uppercase letters check
* @c: Variable text
* Return: Always 0.
*/
int _isupper(int c) 
{
if (C >= 'A' && C <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
