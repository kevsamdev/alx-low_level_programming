#include "main.h"
/**
* mul is a function that checks for a digit
* @a: a - variable
* @b: b - variable
* Return: addition of the multiplicattion
*/
int mul(int a, int b)
{
int mul;
mul = a * b;
return (mul);
}
